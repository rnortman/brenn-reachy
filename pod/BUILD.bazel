# Apache-2.0. The audio device's place in the robot's payload: one blocking
# pre-launch step and one app entry for a binary this build does not produce.
#
# A package with no target that builds anything, because the pod binary is
# brenn-pod's -- it links libusb and libasound and is compiled in that repo's
# arm64 container, so nothing here can name it as a Bazel output. What this
# repo owns is when and where it runs: the launch entries the production and
# recording configs merge, and the staging `tools/build-motion.sh` does from a
# prebuilt artifact.

# The pod's entries, merged into production and recording launchers and absent
# from the motion-only harness twin.
exports_files(["pod_launch.textproto"])
