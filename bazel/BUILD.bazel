# Bazel plumbing for this repo: the included module segment lives here, and
# `include()` resolves it as a label, so this directory has to be a package. The
# tree's own macros also live here, loaded by `//bazel:` label, so the package
# outlives the `include()`.

exports_files(["clockwork_overrides.MODULE.bazel"])
